# 08. Code — Entities: Pizzeria Lifecycle

Part of the tactical design for the **Pizzeria Lifecycle** Bounded Context.

---

`Pizzeria` is the only aggregate in this context (`08_pizzeria_lifecycle_aggregates.md`) and owns a single field (`status`, a plain enum) — no nested entity, no nested value object either. The smallest model in this series, matching its position as the smallest Bounded Context (`08_pizzeria_lifecycle_domain_model.md`).

---

## Open Questions

None at this stage.
